export enum SourceConnector {
	MONGODB = "MongoDB",
	POSTGRES = "Postgres",
	MYSQL = "MySQL",
	ORACLE = "Oracle",
}

export enum DestinationConnector {
	AMAZON_S3 = "Amazon S3",
	APACHE_ICEBERG = "Apache Iceberg",
}
